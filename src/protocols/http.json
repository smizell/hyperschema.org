{
  "definitions": {
    "methods": {
      "type": "array",
      "items": { "$ref": "#/definitions/method" }
    },
    "method": {
      "title": "HTTP Methods",
      "description": "HTTP Methods",
      "type": "string",
      "enum": [
        "OPTIONS",
        "GET",
        "HEAD",
        "POST",
        "PUT",
        "DELETE",
        "TRACE",
        "CONNECT",
        "PATCH"
      ]
    },
    "safeMethod": {
      "title": "Safe HTTP Methods",
      "description": "Safe HTTP Methods",
      "type": "string",
      "enum": [
        "GET",
        "HEAD"
      ]
    },
    "unsafeMethod": {
      "title": "Unsafe HTTP Methods",
      "description": "Unsafe HTTP Methods",
      "type": "string",
      "enum": [
        "POST",
        "PUT",
        "DELETE",
        "PATCH"
      ]
    }
  }
}